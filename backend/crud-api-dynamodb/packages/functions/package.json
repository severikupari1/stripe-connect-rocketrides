{
  "name": "@crud-api-dynamodb/functions",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "sst bind vitest",
    "typecheck": "tsc -noEmit",
    "start:product": "cross-env NODE_OPTIONS='--experimental-specifier-resolution=node' node src/product/app.js --debug",
    "dev:product": "nodemon -L --watch ./../ --exec \"npm run start:product\""
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^20.3.1",
    "sst": "^2.13.8",
    "vitest": "^0.32.2"
  },
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}
